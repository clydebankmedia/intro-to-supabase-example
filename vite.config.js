import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// Standard Vite + React setup. You don't need to change anything here.
export default defineConfig({
  plugins: [react()],
});
